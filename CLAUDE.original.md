# CLAUDE.md — 도우미누오 작업 컨텍스트

> Claude(Cowork)가 매 세션마다 자동으로 읽는 파일. **세션 시작 시 반드시 따라야 할 컨텍스트 포인터**만 둔다.
> 본 파일이 길어지지 않도록, 상세 내용은 다른 문서로 링크. 작업이 끝난 항목은 즉시 정리.

---

## 1. 세션 시작 시 필수로 읽을 문서

코드 작업 전에 **`Read` 로 1회 확인**한다 (이미 읽었던 세션이라도, 디스크 갱신이 잦은 문서이므로 매 세션 1회 다시 읽는다).

1. **[TEMPLATES.md](./TEMPLATES.md)** — 모든 레이어 공통 코드 컨벤션 (§0), 레이어 매트릭스 (§1), 템플릿(§2~§8), workflow recipes (§10), PR 체크리스트 (§9). ES5 스타일, IIFE export, postMessage 프로토콜, storage 키 네이밍, 에러 매퍼 단일화 등 거의 모든 변경에 적용되는 규칙. **거의 항상 영향이 있으므로 항상 먼저 읽는다.**
2. **[OPTIMIZATION_PLAN_R2.md](./OPTIMIZATION_PLAN_R2.md)** — 코드 다이어트 R2 (F30~). Tier 1(F31·F32·F37·F46) 구현·검증 메모는 본 문서 본문이 단일 출처.

새 PR 의 플랜 문서가 추가되면 본 §1 에 **다음 번호** 항목으로 등록한다. 두 파일이 중복으로 알려주는 항목이 있다면 **TEMPLATES.md 가 우선** (프로젝트 전역 규칙 > 단일 PR 의 결정 사항).

---

## 2. 사이드 참조 (필요 시 즉시 Read)

작업 의도에 따라 필요할 때만 읽는다. 매 세션 자동 로딩 대상은 아님.

- **README.md** — 프로젝트 소개, 사용자 관점 기능 목록.
- **PRIVACY.md** — 데이터/트래픽 정책. **새 host_permissions 또는 외부 송신이 들어가는 변경이라면 반드시 갱신.**
- **CHANGELOG.md** — 사용자에게 보이는 변경. **사용자 관찰 가능한 동작이 바뀌면** 한 줄 추가(스토어 제출 직전).
- **[SPEED_FILL_PLAN.md](./SPEED_FILL_PLAN.md)** — `/speed` 자동입력 플로팅 메뉴 PR (Phase 1·2 구현 완료, 2026-05-15). 사후 참조·회귀 메모용.
- **OPTIMIZATION_PLAN.md** — 코드 다이어트/최적화 큰 그림. F (Finding) / G (Guard rail) 번호로 연결되는 회귀 위험 메모가 있음 (예: F10 “flattenSlot 사본 양쪽 갱신”, F18 “share GET 캐시”, F12 “bridge inject 캐싱”).
- **research.md** — 현재 코드의 실제 동작 분석. “왜 이 코드가 이렇게 되어 있나” 가 궁금할 때.
- **BACKLOG.md** — 사용자가 적어둔 아이디어/할 일 큐.
- **할일.md** — 위와 별개로 운영되는 한국어 메모.

---

## 3. 현재 진행 중인 PR

진행 중인 PR 없음. **R2 (F30~F53) 사실상 종결** (2026-05-24). 직전: Tier 5 묶음 3 **F53** — `attackerFormOverride.js` (slug·`base_stats`·`natureKoFromPersonality`). CHANGELOG 1.4.0 반영 완료.

**다음 단계**: 스토어 제출(미커밋 런타임 변경 묶음 커밋 → zip → 업로드). **보류**: F44 (`tbInlineAnnotate` 레거시 키) — OPTIMIZATION_PLAN_R2.md Tier 4.

새 PR 시작하면 본 §3 에 한 줄 — 플랜 파일 경로 + 핵심 만지는 영역 — 추가하고, §1 에도 **다음 번호**로 그 플랜 파일을 등록.

---

## 4. 코드 편집 워크플로우 (Cursor 위임)

**런타임 코드의 실제 편집은 Cursor 가 담당한다.** Claude(본 어시스턴트, Cowork) 는 요구사항 파악·플랜 작성·진단·문서 갱신·회귀 점검 포인트 식별까지 직접 수행하지만, **코드 수정 단계에서는 Edit/Write 로 파일을 직접 만지지 않고 Cursor 에게 줄 prompt 를 한 덩어리로 작성해서 사용자에게 전달**한다. 사용자가 그 prompt 를 Cursor 에 붙여 실행한다.

**범위**

- Cursor 가 수정: `extension/**/*.{js,json,html,css}` (`manifest.json` 포함), `scripts/**/*.js`, 그 외 zip 산출물에 들어가는 모든 런타임/빌드 파일.
- Claude 가 직접 수정 가능: 문서(`.md` — OPTIMIZATION_PLAN_R2.md, TEMPLATES.md, CLAUDE.md, CHANGELOG.md, README.md, PRIVACY.md, research.md, BACKLOG.md, 할일.md 등), 스크래치 폴더의 임시 파일.
- Read·Grep·Glob 은 본 룰의 “편집” 에 해당하지 않음 — 진단·플랜·prompt 작성 단계에서 자유롭게 사용. **prompt 를 정확하게 쓰기 위해 코드를 충분히 읽는 것은 오히려 권장**.

**Cursor 에게 넘길 prompt 의 권장 구성**

1. **목표 한 줄** — 어떤 finding / 사용자 요청 처리인지 (예: “F50 — 인라인 어노 결정력에 화력증강도구 미반영 버그 수정”).
2. **파일·라인 단위 변경 지시** — `extension/<file>` 의 `<function>` (l.<n>~<m>) 을 “이렇게 바꿈”. 여러 파일이면 파일별로 번호.
3. **변경 코드 스니펫** — 짧은 변경은 신규 코드 직접 / 큰 변경은 before·after 블록. 한국어 주석 톤도 그대로.
4. **회귀 점검 포인트** — 사용자 측 수동 검증 절차 (확장 reload → 어떤 페이지에서 어떤 동작을 어떻게 확인).
5. **프로젝트 컨벤션 reminder (필요 시)** — ES5(`var` / IIFE / `function () {}`), 한국어 주석, 기존 export 패턴 유지, drive-by 리팩터 금지 등 Cursor 가 모를 수 있는 본 프로젝트 룰만 짧게 (자세한 건 TEMPLATES.md §0 가 단일 출처).

**예외**

- 사용자가 명시적으로 “네가 직접 수정해” 라고 지시한 경우 — Claude 가 Edit/Write 직접 사용.
- 1~2 라인 hotfix 등 prompt 작성 비용이 비효율적인 경우 — 사용자에게 “Cursor 에 넘길까 / 직접 해도 될까” 한 번 묻고 진행.
- 문서 단독 PR (코드 미수반) — Claude 가 직접 처리.

---

## 5. 핵심 협업 규칙 (TEMPLATES.md §0 와 CLAUDE 프로젝트 룰의 요약)

세션 시작 후 잊기 쉬운 규칙만 짧게 재진술. 자세한 건 TEMPLATES.md §0. **본 규칙들은 Claude 가 짜는 Cursor prompt 안에도 그대로 적용**되어야 한다 — Cursor 가 모르는 본 프로젝트 룰은 §4 의 prompt 구성 #5 에 reminder 로 박는다.

- **요청된 것만 구현**. drive-by 리팩터·미관 정리·새 의존성 도입 금지.
- **파일 스타일 유지**. ES5(`var` / IIFE / `function () {}`), 한국어 주석. 모던 JS 로 일괄 마이그레이션 금지.
- **신규 host_permissions / permissions 추가 금지** (필요하면 사용자 명시 승인 + PRIVACY.md 갱신).
- **어머니 사이트(스마트누오) 트래픽 보호** — OPTIMIZATION_PLAN.md §A. 결과물에 안 들어가는 데이터를 위해 호출 금지, 동일 입력 동일 호출은 SW 라이프타임에 1회.
- **두 곳에 같은 의미의 코드가 있으면 함께 갱신** — 가장 흔한 케이스: `flattenSlot` 사본 (SW `calcPayload.js` ↔ MAIN `teamBuilderBridge.js`).
- **에러 메시지는 한국어 매퍼 (`mapCalcFillError` 등) 단일 통과**. 사용자에게 raw 영문 코드 노출 금지.
- **작업 폴더 표기**: 사용자에게 “the folder you selected” 또는 폴더 이름. 내부 경로(예: `/sessions/...`) 노출 금지.

---

## 6. 코드 작업 기본 원칙 (일반)

프로젝트 무관하게 모든 코드 작업에 적용되는 행동 원칙. 사소한 작업에는 판단껏 완화 가능하지만, 의심될 때는 이쪽으로 기울인다.

- **시작 전에 정리** — 가정·모호함을 침묵 속에 처리하지 않는다. 해석이 둘 이상이면 한쪽을 조용히 고르지 말고 제시. 더 단순한 길이 보이면 먼저 말하고, 막히면 멈추고 무엇이 헷갈리는지 이름 붙여 묻는다.
- **단순함이 기본** — 요청된 것만 구현. 1회용 코드에 추상화 금지, 부탁받지 않은 "유연성/설정 가능성" 금지, 일어날 수 없는 시나리오에 대한 방어 코드 금지. 200줄을 50줄로 줄일 수 있으면 다시 쓴다. "시니어 엔지니어가 보면 과한가?" — 그렇다면 단순화.
- **외과적 수술** — 만져야 하는 곳만 만진다. 인접 코드·주석·포맷을 "개선" 하지 않는다. 멀쩡한 걸 리팩터하지 않는다. 자기 변경이 만든 import/변수/함수 고아만 정리하고, 사전부터 있던 데드코드는 보고만 하고 두라. 변경된 모든 라인은 요청과 직결되어야 함.
- **검증 가능한 목표** — "고친다/추가한다" 가 아니라 "이 테스트가 통과한다 / 이 동작이 관찰된다" 로 성공 조건을 먼저 정의. 멀티스텝이면 단계별 검증 포인트를 짧게 명시. 약한 목표("동작하게 만들어")는 끝없는 재확인을 부르고, 강한 목표는 독립적으로 루프 돌 수 있게 해준다.

---

## 7. 본 파일 자체의 운영

- 본 PR 이 끝났을 때, 본 파일 §3 을 정리하고 새 PR 플랜 포인터로 갱신.
- **코드 컨벤션** 변경이 정착되면 본 파일이 아니라 TEMPLATES.md 를 갱신 (이 파일은 “포인터 + 현재 작업 상태 + 워크플로우 메타규칙” 만, 코드 컨벤션 본체는 TEMPLATES.md).
- **워크플로우/협업 메타규칙** 변경(예: Cursor 위임 정책 같은 §4 류) 은 본 파일이 단일 출처 — TEMPLATES.md 에 중복 두지 않는다.
- 길이 가이드: §1, §3 합쳐서 한 화면 이내 유지. 길어지면 분리.
